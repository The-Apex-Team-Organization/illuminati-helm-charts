apiVersion: v1
kind: Service
metadata:
  labels:
    run: frontend
  name: frontend
  namespace: illuminati
spec:
  ports:
  - port: 80         
    protocol: TCP
    targetPort: {{.Values.containerPort}}
  selector:
    run: frontend
  type: ClusterIP
